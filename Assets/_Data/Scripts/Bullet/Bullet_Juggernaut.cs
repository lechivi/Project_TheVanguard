using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class Bullet_Juggernaut : Bullet
{
    private TrailRenderer tracer;
    public override void Deactive()
    {
        this.gameObject.SetActive(false);
        isActive = false;
        intitialPosition = Vector3.zero;
        intitialVelocity = Vector3.zero;
        tracer.emitting = false;
        tracer.Clear();

    }

    public override void Active(Vector3 position, Vector3 velocity)
    {
        this.gameObject.SetActive(true);
        isActive = true;
        time = 0f;
        intitialPosition = position;
        intitialVelocity = velocity;
        tracer.emitting = true;
        tracer.AddPosition(position);
    }
}*/
